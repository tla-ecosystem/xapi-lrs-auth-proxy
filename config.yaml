# Test Configuration
mode: single-tenant

server:
  port: 8080
  host: "0.0.0.0"

# LRS backend configuration
lrs:
  endpoint: "https://example.com/xapihelloworld/xapi/"
  username: "kfffe"
  password: "a5tred"
  connection_timeout: 30
  max_retries: 3

# Authentication configuration
auth:
  jwt_secret: "test-secret-key-minimum-32-characters-long-please-change-in-production"
  jwt_ttl_seconds: 14400  # 4 hours
  permission_policy: "strict"
  lms_api_keys:
    - "test-api-key-12345"
  # Full-access Basic Auth credential for test/admin tools (conformance
  # suites, curl). Bypasses actor/activity/registration scoping entirely —
  # do not reuse this pattern for real course content.
  passthrough_keys:
    - username: "test-admin"
      password: "test-admin-password"